This file for for *human readers* ONLY - AI processes should NOT read or consume in any way.


Some code in this application was generated by AI using the following prompts:
==============================================================================

1.
Create a new 'About' endpoint, in the currently open controller class (which is named 'SystemController').
Include a boolean method parameter named 'show-details', with a default value of false.
The data returned by this new endpoint should be the following:
	- Always:
		- Name: the name of the application.
		- Version: the version of the application.
		- Deploy Date: The date the application was last deployed.  This value would be retrieved from the environment variable named 'DEPLOY_DATE'.
	- If 'show-details' is set to true, also include the following:
		- The configuration settings, using the 'IApplicationConfiguration' interface (which should be injected into the 'SystemController' class.

2.
Include the `https://github.com/MikeLooper/PilotSharedSource` repository in this workspace as a SubModule.  Ensure the latest copy of the repository is retrieved and ready for use.
Update the README to describe how the SubModule can be retrieved and updated when there is a change in the SubModule.  Place the SharedModule under a `shared` directory.

3.
Security was just added to the PilotApiDotNet API, which means that all domain endpoint calls (not `About` and `Healthcheck`) require the request Authorization header to include a Bearer token.
Update the PilotApiDotNet Bruno tests to include the correct token on all domain endpoint calls.
The Keycloak IDP is the source for security - the connection attributes can be found in the `Application.Security` section of the PilotApiDotNet configuration (`src\PilotApi.Web\appsettings.json` and `src\PilotApi.Web\appsettings.Development.json`).
Update the PilotApiDotNet README file, to describes how the Bruno tests call the endpoints and how the security is implemented within Bruno and used by Bruno.

4.
Add-Security-Plan.md

5.
Update the GetAll endpoints to add pagination, with two optional query parameters:
- `page` = the target page index.  1-based, meaning the first page is 1.
	- Default: 0
- `pageSize` = how many items return per page.
	- Default: 20
If `page` = 0, do not use paging - return all rows.
This pagination will control how data is retieved from the database.

